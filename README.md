# backup.c

A small POSIX C utility that copies a file to a \`.bak\` file and preserves the source access and modification timestamps.

## Build and run

\`\`\`sh
cc backup.c -o backup
./backup path/to/file
\`\`\`

This is systems-programming coursework intended for Unix-like systems.
